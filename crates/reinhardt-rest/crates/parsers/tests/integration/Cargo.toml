[package]
name = "reinhardt-integration-tests"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
reinhardt-parsers = { path = "../.." }
# TODO: Add reinhardt-http when implemented
# TODO: Add reinhardt-server when implemented
bytes = "1.0"
tokio = { version = "1.0", features = ["full"] }

[dev-dependencies]
