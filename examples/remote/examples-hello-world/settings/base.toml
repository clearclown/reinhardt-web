# Base settings for examples-hello-world
# These settings are common across all environments

debug = true
secret_key = "your-secret-key-for-development-only"

# Minimal configuration
use_i18n = false
use_tz = false
append_slash = false

language_code = "en-us"
time_zone = "UTC"
