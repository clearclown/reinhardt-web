//! URL configuration for issues app (GraphQL)

use reinhardt::UnifiedRouter;

pub fn url_patterns() -> UnifiedRouter {
	let router = UnifiedRouter::new();

	// TODO: Add GraphQL endpoint here

	router
}
